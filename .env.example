# Copy to .env and adjust. With docker compose the default below works as is.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/e76
# Port for `npm run api` (optional, defaults to 3000)
PORT=3000
